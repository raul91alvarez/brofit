import { StateClientPipe } from './state-client.pipe';

describe('StateClientPipe', () => {
  it('create an instance', () => {
    const pipe = new StateClientPipe();
    expect(pipe).toBeTruthy();
  });
});
