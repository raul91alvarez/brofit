import { StateUsersPipe } from './state-users.pipe';

describe('StateUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new StateUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
